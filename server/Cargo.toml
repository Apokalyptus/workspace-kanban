[package]
name = "kanban-server"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tiny_http = "0.12"
time = { version = "0.3", features = ["formatting"] }
